import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function RemindersPage() {
  return (
    <PlaceholderPage
      title="Reminders"
      phase="Phase 5a"
      description="A real date/time picker for creating a reminder, plus a list/cancel view -- the picker's 'when' payload mirrors the /menu wizard's own step data, just rendered as a native picker instead of stepper buttons."
    />
  );
}
