import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function ConvertPage() {
  return (
    <PlaceholderPage
      title="Convert"
      phase="Phase 5c"
      description="A file drop zone plus a target-format picker, calling the same one-shot conversion path as the /convert <format> caption command."
    />
  );
}
