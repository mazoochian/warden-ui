import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function BotViewPage() {
  return (
    <PlaceholderPage
      title="Bot View"
      phase="Phase 6"
      description="Live incoming messages for a chat (streamed over WebSocket) plus a confirmation-gated compose box to send as the bot. The most sensitive surface in the whole panel -- see ARCHITECTURE.md §8 before building this."
    />
  );
}
