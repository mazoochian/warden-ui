import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function AuditLogPage() {
  return (
    <PlaceholderPage
      title="Audit Log"
      phase="Phase 7"
      description="Every mutating API call's trail -- who, what, when -- filterable by action/account/date. The backend has been writing to audit_log since Phase 0; this is just the browsing UI."
    />
  );
}
