import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function GroupsPage() {
  return (
    <PlaceholderPage
      title="My Groups"
      phase="Phase 4"
      description="Chats where you're a live platform admin (or every chat, if you're a bot admin/owner), with a per-group settings form -- persona, magic word, digest, thinking override."
    />
  );
}
